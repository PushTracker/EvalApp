import { Component, Inject } from '@angular/core';

// libs
import { AppBaseComponent } from '@maxmobility/web';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html'
})
export class AppComponent extends AppBaseComponent {
  constructor(

  ) {
    super();
  }
}
